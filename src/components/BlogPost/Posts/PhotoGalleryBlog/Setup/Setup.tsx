import Paragraph from '../../Paragraph/Paragraph';
import './Setup.css';

const Setup = () => {

    return (
        <section className="photo-gallery-setup-section section-container">
            <Paragraph>
                Before start coding, we will first deal with some house-keeping, like setting up a react 
                project and the appropriate folder structure. Setting up a react project using a
                TypeScript template is relatively easy. 
            </Paragraph>
        </section>
    );
};

export default Setup;